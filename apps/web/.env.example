# The anon/publishable key is safe to expose in the browser bundle -- Row
# Level Security controls what it can actually read/write (see
# supabase/migrations/0001_operational_schema.sql). Never put the service
# role key here; that belongs only to canon-service.

NEXT_PUBLIC_SUPABASE_URL=https://dghkxaclaeluheahdsne.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_8W0y2ad9r8e7t1UZedZytQ_HZQU7vGZ

# Private-network URL of canon-service once deployed on Railway (used for
# admin actions: AI-Parse, ratify, extract). Not needed for the reader-facing
# vertical slice.
CANON_SERVICE_URL=
